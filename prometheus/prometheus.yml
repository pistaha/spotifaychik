global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: app
    metrics_path: /metrics
    dns_sd_configs:
      - names:
          - app
        type: A
        port: 8080

  - job_name: nginx
    static_configs:
      - targets:
          - nginx-exporter:9113
